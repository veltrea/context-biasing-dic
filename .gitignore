/target
**/*.rs.bk
Cargo.lock.orig

# 生成物（辞書・別ログ）。素材であり成果物なのでコミットしない。
/*.dict
/*.txt.dict
/dict.txt
/reject.txt
/out/

# harvest のキャッシュ。取得した記事本文・生成音声・認識結果を含むため
# 決してコミットしない（SPEC §12・§15）。
harvest_cache/

# デザインモック（Stitch 生成の中間素材）。実装は app/ にあり、project ID から再生成も可能なのでコミットしない。
/design/

# 監査・判断の手元記録。プライバシー詳細を含むため公開リポジトリには載せない。
/PRIVACY-AUDIT.md

# OS / editor
.DS_Store
*.swp
